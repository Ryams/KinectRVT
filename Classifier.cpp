#include "Classifier.h"

int classify(const Mat& colorIn, const Mat& depthIn) {
	return 1; // should be enum;
}

void printClass(const int& classification) {
	cout << "Class: " << classification << endl;
}